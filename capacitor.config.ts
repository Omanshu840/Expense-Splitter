import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.expense.splitter',
  appName: 'expense-splitter',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
